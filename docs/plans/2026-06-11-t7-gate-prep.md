# T-7 gate prep — 2026-06-12 (pilot opens 2026-06-19)

> Execution prep for `docs/plans/2026-06-07-plan-n-verification-checklist.md`.
> The gate has a hard deadline: **the Google Form bridge cutoff decision must
> be recorded by 2026-06-12**. If the bridge subsection is not green by then,
> disable the bridge and run the pilot on native registration + CSV import.

## State as of prep (2026-06-11)

| Item | State |
|---|---|
| `main` | `1464ac1` — Plan N merged (`d4cc509`); UI lane #71/#73/#74/#75/#76 merged |
| PR #77 (Plan O — bridge Sheet ops) | OPEN, CI green, mergeable; final review approved; frontend-only |
| Prod backend health | ✅ verified 2026-06-11: `{"status":"ok","version":"0.1.0","database":"ok"}` |
| Prod frontend | ✅ `/login` returns 200 (Vercel deploy lag caveat — re-verify after merges) |
| Linear | no Eventgate project — `docs/plans/` is the planning surface |

## Pre-requisites (before running the gate)

1. **Merge PR #77** — the Sheet-menu workflow it adds is exactly what the
   bridge rehearsal exercises; rehearsing pre-merge would validate a stale
   script. Owned by the session that authored it (or merge here once that
   session confirms it's done).
2. **Confirm prod deploys picked up the merges** — backend auto-deploys on
   `backend/**` push (none of #75–#77 touch backend, so backend should still be
   the Plan N build — verify release ≥ the Plan N merge); frontend is Vercel
   auto-deploy (NOT inspectable from agent sessions; verify by loading the app
   and seeing the skeletons/bridge card live).
3. `docker start eventgate-postgres-1` for local backend test runs.
4. `source ~/.nvm/nvm.sh && nvm use 20` for frontend gates.

## Checklist execution map

Legend: **[agent]** runnable by a Claude session · **[user]** needs Vinei
(real accounts/devices/Google) · **[CC]** needs Click Cam coordination.

### Section 0 — code and deploy state
- [agent] local main vs remote; backend pytest + mypy; frontend four-command
  gate; prod health curl. (Prod health already green 2026-06-11.)

### Section 1 — required ingress
- [user] native public registration → guest + QR email (needs a real inbox;
  sender is still `onboarding@resend.dev`, sandbox-limited to
  vinei.dev@gmail.com).
- [agent] CSV import preview + mixed-file commit counters (staging or prod org).

### Section 1 (optional) — Google Form bridge subsection
- [user] **Real-Sheet rehearsal** (the Plan O PR's declared outstanding item):
  install Apps Script on the actual Click Cam response Sheet while the bridge
  is DISABLED → enable → one test submission → expect 1 guest + 1 QR email →
  replay the same submission → expect no duplicate guest/email → submission
  missing required email → expect rejection + audit row → disable bridge →
  expect clean rejection, no guest.
- Rehearsal order and retry limits: `docs/runbooks/google-form-bridge-apps-script.md`.

### Section 2 — door path
- [user] device enroll, PIN unlock, cache prime, pre-registered scan, duplicate
  scan, offline queue/replay, helpdesk escalation (needs a phone + camera).

### Section 3 — walk-in path
- [user] display QR, claim, info form, capacity boundary, blocked re-scan
  reminder.

### Section 4 — operational readiness
- [agent] Sentry prod intake, Fly app/worker/beat health, Redis reachability.
- [user] Telegram CTA if configured.
- [CC] printed fallback list confirmed with Click Cam.
- [user] **record the bridge cutoff decision** (template below).

## Bridge cutoff decision record (fill by 2026-06-12)

```
Decision:  enabled | disabled
Owner:
Timestamp:
Reason:
```

Record it in this file (or the verification checklist) and commit.

## Suggested order for 2026-06-12

1. Merge #77 → confirm Vercel picked it up.
2. Agent runs Section 0 + Section 4 agent-items, reports.
3. Vinei runs the bridge rehearsal (Section 1 optional) early — it gates the
   cutoff decision and has the most unknowns (live Apps Script auth/trigger
   behavior).
4. Vinei runs Sections 1/2/3 user-items (door + walk-in can reuse the
   rehearsal event).
5. Record the cutoff decision + check off the checklist; note any findings in
   `docs/plans/improvement-and-findings-logs.md`.
