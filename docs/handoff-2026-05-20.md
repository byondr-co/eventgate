# Eventgate — Project Handoff (2026-05-20)

> Last updated: **2026-06-07** — Plan N implemented and verified locally on
> `topic/plan-n-google-form-bridge`. The historical Plan F baseline is retained
> below, but the current resume state is this Plan N overlay plus
> `docs/handoff-2026-06-03-post-plan-l-state.md`.

Use this as the system prompt / context for a fresh Claude project chat. It captures what's built, what's deployed, and what's next so we can resume without rebuilding context.

---

## Current resume context — 2026-06-07 Plan N

Plan N added the optional Click Cam Google Form bridge path. It is implemented
and verified in the isolated worktree
`.claude/worktrees/plan-n-google-form-bridge` on branch
`topic/plan-n-google-form-bridge`; it is not yet merged or deployed.

Read these current docs before new work:

1. `docs/handoff-2026-06-03-post-plan-l-state.md` — latest merged production
   context after Plan L + pilot-feedback.
2. `docs/plans/2026-06-07-plan-n-pilot-reliability-google-form-bridge.md` —
   implementation plan.
3. `docs/plans/2026-06-07-plan-n-verification-checklist.md` — T-7/T-3/T-1
   verification gate for pilot reliability plus optional bridge.
4. `docs/runbooks/google-form-bridge-apps-script.md` — customer install guide.

### Plan N branch contents

- New backend app `apps.integrations` with `GoogleFormBridge` and
  `GoogleFormSubmission`, hashed shared secret, create/rotate secret helpers,
  org/event derivation, and migrations.
- Admin API:
  - `GET/POST /api/v1/orgs/<org>/events/<event>/integrations/google-form-bridge/`
  - `GET/PATCH /api/v1/orgs/<org>/events/<event>/integrations/google-form-bridge/<bridge_id>/`
  - `POST .../<bridge_id>/rotate-secret/`
- Public webhook:
  `POST /api/v1/integrations/google-forms/<bridge_id>/submissions/` with
  `X-Eventgate-Bridge-Secret`.
- Submission processor: validates mapping, rejects disabled bridges, scopes
  duplicate detection by event/org/email, uses an event/email advisory lock,
  creates or updates guests through `register_guest()`, queues QR email only on
  accepted new guests, records accepted/duplicate/updated/rejected submissions,
  and writes integration audit rows.
- Frontend API hooks in `frontend/lib/google-form-bridge.ts`.
- Event settings card for create, one-time secret display, secret rotation,
  field mapping add/update/remove, required-field enable guard, webhook URL, and
  Apps Script snippet.
- Event settings page now surfaces the Google Form bridge card.
- Pilot runbook now includes a Google Form bridge smoke that must run no later
  than **2026-06-12** for the bridge go/no-go; T-3/T-1 reruns are only for an
  enabled bridge.

### Plan N verification already run

- Backend: `/Users/vinei/.local/bin/uv run pytest -q` -> **367 passed**.
- Backend: `/Users/vinei/.local/bin/uv run mypy apps config` -> clean across
  **174 source files**.
- Backend: ruff, `makemigrations --check --dry-run`, and `manage.py check` all
  clean.
- Frontend: `pnpm test` -> **57 files / 265 tests passed**.
- Frontend: `pnpm exec tsc --noEmit`, `pnpm lint`, and `pnpm format:check`
  passed. Lint still reports 3 pre-existing `<img>` warnings outside Plan N.
- Scope scan for deferred Google integration work found no OAuth, Pub/Sub,
  Forms API watch, refresh-token, custom-domain, or billing implementation in
  Plan N.
- Final full-branch review found no critical code/security blockers. The
  canonical handoff and manual retry guide were updated as closeout fixes.

### Current Plan N follow-ups

- Open a PR / merge the branch, then run CI and deploy checks.
- Rehearse the bridge with the actual Click Cam Google Form response Sheet:
  install Apps Script while disabled, enable before the test submission, confirm
  one guest + one QR email, confirm replay idempotency, then record the cutoff
  decision.
- If the Google Form bridge is not green by **2026-06-12**, disable it and use
  Eventgate native registration or CSV import.
- Nice-to-have but not a merge blocker: add explicit admin API denial tests for
  viewer/non-member/anonymous, and align the dashboard Apps Script snippet with
  the runbook's 5xx retry helper.

---

## Who & What

**You're working with Vinei** (vinei.dev@gmail.com, GitHub `vineidev`) on **Eventgate** — a SaaS for fast, paperless event entrance, targeting Southeast Asian event organizers (Cambodia-first). The product re-implements an existing Google Apps Script + Sheets MVP as a production multi-tenant SaaS.

The strategic brief lives at `docs/brief.md`. Read it once if you haven't.

---

## Where things live

```
Local repo:    /Users/vinei/Projects/eventgate/
GitHub:        git@github.com:vineidev/eventgate.git  (private)
Plans:         /Users/vinei/Projects/eventgate/docs/plans/
Brief:         /Users/vinei/Projects/eventgate/docs/brief.md

Legacy MVP (do NOT modify — still serves pilot #1):
               /Users/vinei/Projects/Paperless-Pre-check-in/
```

### Deployments (Singapore region)

| Service                | URL / Provider                                                                                                                                                      |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend (Django + DRF) | https://eventgate-backend-staging.fly.dev (Fly.io)                                                                                                                  |
| Frontend (Next.js)     | https://frontend-five-lovat-94.vercel.app (Vercel; alias `frontend-five-lovat-94`)                                                                                  |
| Postgres               | Neon, `ap-southeast-1`                                                                                                                                              |
| Redis                  | Fly Redis (Upstash-backed), `fly-eventgate-redis-staging.upstash.io`                                                                                                |
| Errors                 | Sentry (DSN in Fly secrets)                                                                                                                                         |
| Email                  | Resend (API key in Fly secrets); current sender `Eventgate <onboarding@resend.dev>` (sandbox — only sends to vinei.dev@gmail.com until a sender domain is verified) |

All sensitive values live in Fly secrets (`flyctl secrets list --app eventgate-backend-staging`) and Vercel env (`vercel env ls --token <VERCEL_TOKEN> --scope vineidev-4891s-projects`). **Do not commit credentials to git.** API tokens that were pasted in prior chat sessions should be rotated when convenient.

---

## What's complete

### Plan A — Foundation (commits `02acb2b` → completion log)

Monorepo (`backend/` + `frontend/`), Django 5 + DRF skeleton, Next.js 16 + React 19 + Tailwind v4 + shadcn/ui, Postgres + Redis via docker-compose (local) and Neon + Fly Redis (staging), pre-commit hooks (ruff, ruff-format), GitHub Actions CI for both apps, Sentry, end-to-end healthcheck (`/api/health/`) returning live DB status.

### Plan B — Accounts, Orgs, Memberships, Magic-link

Custom email-only `User` model (no usernames, no passwords by default), `MagicLinkToken` with SHA-256-at-rest + 15-min TTL + single-use, JWT in httpOnly cookies (`CookieJWTAuthentication`), `Organization` + `OrganizationMembership` + `Invite` models, `OrgScopedModel` abstract base + `IsOrgMember` / `HasOrgRole` permissions, members + invite endpoints. Frontend: `/login`, `/auth/callback`, `/`, `/orgs/new`, `/orgs/[slug]`, `/orgs/[slug]/members`, `/invites/[token]`.

### Plan C — Events, Public Registration, QR + Email

`apps.events` (`Event` + `RegistrationField` with EN+KM labels) and `apps.guests` (`Guest` inheriting `OrgScopedModel`, separate `entry_status` / `info_status` per brief Appendix A). Public anonymous registration at `POST /api/v1/e/<org>/<event>/register/` returns guest_id only (token never echoed). QR rendered on demand via `segno` at `GET /api/v1/guests/<id>/qr.png?token=<raw>` with constant-time compare. Magic-link AND QR delivery routed through Celery tasks (run eagerly in web process — see below). Resend wired conditionally. `NotificationDispatch` model logs every send. Frontend: public `/e/[orgSlug]/[eventSlug]/register` + `/registered/[guestId]`, organizer event create wizard + events list + event detail + form builder + guest list. `next-intl` with EN + KM message bundles.

### Plan D — Walk-in Flow + Pre-registered Scanner PWA + Celery Worker + Public Event Detail

New backend apps: `apps.audit` (skeleton — append-only `AuditEvent`, `write_audit()` is the only sanctioned writer; UI viewer lands in Plan F), `apps.devices` (`ScannerDevice` with SHA-256-at-rest device tokens + role gating, `EventPinSession` 8h TTL, `DeviceTokenAuthentication` + `SessionTokenAuthentication` reading the `Authorization` header), `apps.checkins` (one hot-path endpoint `POST /api/v1/checkins/` with `pg_advisory_xact_lock` + Redis-backed idempotency + audit), `apps.walkins` (display-next + public claim + public info; partial unique index `one_displayed_walkin_per_scope` is the backstop). New modules: `apps.guests.transitions` (single source of truth for `entry_status` / `info_status` moves), `apps.common.locks`, `apps.common.idempotency`. Existing `apps.events` gained PIN set/rotate (bcrypt-hashed, owner/admin only) + `PublicEventDetailView`. Frontend `(scanner)` route group lives at `/scanner/{enroll,unlock,scan,walkin}` with PWA manifest + minimal static-asset service worker (Workbox + IndexedDB defers to Plan E). Public `/e/<org>/<event>/{claim,info}/<token>/` pages. Camera uses `BarcodeDetector` with a manual-entry fallback for browsers that lack it. Walk-in QR rendered via `qrcode.react` SVG at 2048px scaled via CSS to 85% of the viewport. Celery worker process group on Fly (`[processes].worker`) running its own Machine; `CELERY_TASK_ALWAYS_EAGER` Fly secret unset; `PUBLIC_BASE_URL` Fly secret set so walk-in QRs encode the right host.

### Plan E — Offline Scanner Sync (Workbox + IndexedDB + Mutation Queue + Conflict Routing)

Replaces Plan D's minimal static-only SW with a Workbox-composed service worker bundled via esbuild (`sw-src/sw.ts` → `public/sw.js` via `scripts/build-sw.mjs`, wired into `pnpm build`). Adds a Dexie 4 IndexedDB layer at `lib/scanner/db.ts` with three stores: `guests` (minimal projection — `id`, `entry_token`, `full_name`, `email`, `guest_type`, `entry_status`, `info_status`, `updated_at`), `mutation_queue` (offline check-in writes with status enum `pending | in_flight | completed | conflict | failed | escalated`, exponential backoff 1s→60s × 8 max attempts), `meta` (key/value for sync cursor + ETag).

The cache primes on PIN unlock from a new backend endpoint `GET /api/v1/orgs/<org>/events/<event>/guests/sync/?since=<iso>` (scanner-session auth, ETag-aware, returns 304 when nothing changed). It refreshes on `online` / `visibilitychange` / a 5-minute interval. The scan page branches on `navigator.onLine`: online uses the existing `POST /api/v1/checkins/`; offline looks up the token in IndexedDB, renders an optimistic outcome (success / duplicate / "will validate on reconnect"), and enqueues the mutation. A separate sync loop (`online` + `visibilitychange` + 30s heartbeat) drains the queue.

Server-authoritative conflict resolution: when a queued mutation reconnects and the server returns 409 from a _different_ device/gate, the client marks it `conflict` and the server emits an additional `checkin.conflict` audit row alongside the existing `checkin.duplicate`. A new `POST /api/v1/scanner/escalations/` endpoint (in new `apps.scanner` Django app) writes a `checkin.help_desk_escalation` audit row when the staffer taps "Send to help desk" on the new `/scanner/escalations` page. Plan F builds the inbox that reads those rows.

Scanner header gains: an "Install" PWA prompt button (capturing `beforeinstallprompt`), a "⚠ N conflict(s)" link to `/scanner/escalations` when conflicts exist, and "● offline — N queued" pill. Persistent below-header banner appears when offline. Browser Sentry SDK init dynamically loads from `sentry.client.config.ts` only on `/scanner/*` routes (first frontend Sentry land — needs `NEXT_PUBLIC_SENTRY_DSN` set on Vercel before it activates).

**Plan E ops cleanups (Task 0 wave):** `worker.restart=always` declared explicitly in `fly.toml`; **Vercel `rootDirectory` set to `frontend` via REST API** (root cause of "auto-deploy not working" during Plan D: builds were firing but failing with `NEXT_NO_VERSION` because `rootDirectory` was null — finding documented at `PLAN_E_TASK_0B_FINDINGS.md`); `prettier` pinned exact (no caret) in `frontend/package.json`; all 7 Plan-D-era `# type: ignore` comments replaced with real annotations (parameterized `BaseUserManager["User"]`, `models.Manager["Organization"]`, `TYPE_CHECKING` forward refs, two `cast(timedelta, settings.SIMPLE_JWT[...])`).

### Plan F — Help-desk Inbox + Audit Viewer + Dashboard Polling + DB Append-only

19 implementation tasks across 24 commits (`759a6f7` → `9dac477`) + 1 plan doc (`5dcdf7c`), 5 verification-driven follow-ups (F+1 through F+5), and a verification walkthrough that caught 2 inline correctness bugs (now fixed). **Pilot-ready** per the verification findings at `docs/plans/2026-05-22-plan-f-verification-findings.md`.

**New backend app `apps.helpdesk`** with `HelpDeskTicketState` model — hybrid 1:1 to `AuditEvent` via `OneToOneField`. The immutable audit signal (`checkin.help_desk_escalation` row) stays where it lives; the mutable claim/resolve state lives in the side table. Backfill migration creates state rows for any pre-existing escalation audit rows. After F+1, `event` is nullable to match `AuditEvent.event`'s nullability, and the backfill skips event-less rows.

**DB-level append-only enforcement on `audit_auditevent`** (Task 1): `BEFORE UPDATE OR DELETE` trigger raising `IntegrityError audit_auditevent is append-only (TG_OP=...)` + `REVOKE UPDATE, DELETE` on the current_user role. Belt and suspenders — trigger catches any connection (incl. `flyctl ssh ... psql`); REVOKE adds role-level defense in depth.

**Transitions table extension** (Task 11): `manual_review → checked_in` and `manual_review → voided` for both `pre_registered` and `walk_in` guests. Help-desk override authority per brief Appendix A row 8.

**New endpoints:**

- `GET /api/v1/orgs/<slug>/events/<event>/helpdesk/tickets/` — paginated list, `?status={open,claimed,resolved,open_or_claimed}` filter, ETag/304 (F+4 includes `Count` so row-removal-from-filter changes the hash)
- `POST .../helpdesk/tickets/<id>/{claim,release,resolve}/` — state transitions; each emits a `helpdesk.{ticket_claimed,ticket_released,ticket_resolved}` audit row; `resolve` accepts `action` ∈ `{approve_checkin, resolved_with_note, void, escalated_to_manual_review}`
- `POST .../helpdesk/manual-review/<guest_id>/resolve/` (Task 11) — transitions manual_review guest to checked_in or voided; emits `helpdesk.manual_review_resolved` audit row
- `GET .../audit/?action_prefix=…` — read-only audit list with prefix filter + ETag/304 (uses `Count("id")` because `AuditEvent.id` is UUID; `Max(uuid)` isn't supported by Postgres)
- `GET .../stats/` — 7-field counts endpoint with ETag/304 for 5s polling, aggregates `max(updated_at)` across guests + ticket states + audit events
- `GET .../guests/?entry_status=manual_review` (Task 11) — Plain old query-param filter on the existing GuestListView

**Send-to-manual-review producer (F+3):** 4th resolve action on the ticket detail pane. When the underlying guest is in `registered_not_arrived` or `displayed`, this resolves the ticket AND transitions the guest to `manual_review`. Emits 2 audit rows: `helpdesk.ticket_resolved` (`action=escalated_to_manual_review`) + `helpdesk.manual_review_escalated`. Without it, the Manual review chip would be perpetually empty in prod.

**Frontend dashboard surfaces:**

- `/orgs/<slug>/events/<event>/helpdesk` — 5-chip inbox (`Open`/`Claimed`/`Resolved`/`Manual review`/`All`), 40%/60% list+detail layout. Detail pane: claim/release + 4 resolve buttons. Polls every 5s via SWR. F+5 adds `key={selected.key}` to TicketDetail/ManualReviewDetail to force remount per selection (notes textarea cross-contamination fix).
- `/orgs/<slug>/events/<event>/audit` — read-only paged table, 4 prefix chips (`All`/`Check-ins`/`Walk-ins`/`Help desk`), 10s polling, TanStack Query
- **6-tile live counts widget** on the event detail page (5s polling): Checked in / Pending / Walk-in QR shown / Manual review / Open escalations / Conflicts (15m). Warning/danger tone coloring on non-zero exception tiles.
- "Help desk" + "Audit" links added to the event-detail button row

**F+2 client-side ETag round-trip:** `frontend/lib/etag-fetch.ts` provides `createEtagCache()` — per-URL Map of `{etag, body}`. Each polling client (`useTickets`, `useAuditEvents`, `useEventStats`) instantiates its own at module scope. On 200, stores ETag + body; on 304, returns prior body without re-parse. Verification confirmed every poll endpoint returns 304 when state is unchanged.

**Plan F Task 0 wave — Plan E carryover items cleared:**

- Verification-checklist patches to `docs/plans/2026-05-21-plan-e-verification-checklist.md`
- Backend auto-deploy GHA on push to `main` touching `backend/**` (`.github/workflows/deploy-backend.yml`)
- iOS install banner (`display-mode: browser` + iOS UA detection, dismissable + persistent via `localStorage["scanner:ios-install-banner-dismissed"]`)
- `in_flight` mutation reaper at PWA startup (`reapStaleInFlight()` resets >5min-stale rows to `pending`)
- Dedupe `enqueueCheckin` by `target_token` (short-circuit on existing active row)
- Retry-failed affordance on `/scanner/escalations` (`FailedRow` component with Retry button; clicking calls `retryFailedMutation()` which resets status→pending, attempts→0)
- Online cache update after check-in success (Task 0g: `markCachedGuestCheckedIn(rawToken)` after `result.kind === "success"`)
- Per-IP rate limit on `POST /api/v1/devices/enroll/` (10/min via DRF `SimpleRateThrottle`, cache key `throttle:device_enroll:<ip>`)

**Verification-driven fixes (the F+ wave):**

| SHA       | Subject                                                                                                                                 |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `f13d8ff` | F+1: `fix(helpdesk): event nullability matches audit, backfill skips null-event rows`                                                   |
| `481203c` | F+2: `perf(frontend): send If-None-Match on /stats /helpdesk/tickets /audit polls`                                                      |
| `b47e169` | F+3: `feat(helpdesk): send-to-manual-review action closes the producer gap`                                                             |
| `9e2b4f7` | `chore(helpdesk): merge migration reconciling F+1 nullable-event and F+3 resolution-action widening`                                    |
| `ed50eb1` | `style(frontend): prettier-format event-stats.ts arrow expression` (caught by CI on first push)                                         |
| `860ba40` | **F+4 (caught during verification)**: `fix(helpdesk): ETag input includes Count so polling detects row removal from filter`             |
| `9dac477` | **F+5 (caught during verification)**: `fix(helpdesk): remount TicketDetail/ManualReviewDetail per selection so notes textarea resets`   |
| `d9bbabd` | `ops(fly): auto-run migrations on deploy via release_command` (verification surfaced that migrations had never auto-run on Fly deploys) |

See `docs/plans/2026-05-22-plan-f-verification-findings.md` for the full verification report.

### Plan A/B/C deploy-time fixes (already shipped)

- CORS `Allow-Credentials: true` (needed for `fetch(credentials: "include")`)
- JWT cookies `SameSite=None; Secure=true` on Fly
- `CELERY_TASK_ALWAYS_EAGER=true` Fly secret (no worker process yet)
- Vercel `/api/*` rewrite to Fly (so cookies are same-origin on `vercel.app`)
- `trailingSlash: true` + explicit destination slash in next.config (Django's `APPEND_SLASH`)
- `ALLOWED_HOSTS=*` on Fly (Consul health-check uses internal hostname)

### What's verified end-to-end on staging

```
GET  /api/health/                                                       → 200
POST /api/v1/auth/magic-link/request/  {"email": …}                     → 204 + Resend email
POST /api/v1/auth/magic-link/consume/  {"token": …}                     → 200 + JWT cookies
GET  /api/v1/auth/me/                                                   → 200 user JSON
POST /api/v1/orgs/                       {"name": …}                    → 201 (creator auto-owner)
POST /api/v1/orgs/<slug>/events/         {"name": …, "slug": …}         → 201 + preset fields seeded
POST /api/v1/orgs/<slug>/invites/        {"email": …, "role": …}        → 201 + invite email
POST /api/v1/auth/invites/<token>/accept/                               → 200 + membership
POST /api/v1/e/<org>/<event>/register/   {"name", "email", "phone…"}    → 201 + QR-attached email
GET  /api/v1/orgs/<org>/events/<event>/guests/                          → 200 list
GET  /api/v1/guests/<id>/qr.png?token=<raw>                             → 200 image/png 629×629

# Plan D
GET  /api/v1/e/<org>/<event>/                                           → 200 public event detail (anonymous)
POST /api/v1/orgs/<org>/events/<slug>/pin/set/    {"pin": "…"}          → 204
POST /api/v1/orgs/<org>/events/<slug>/pin/rotate/                       → 204
POST /api/v1/orgs/<org>/devices/                  {"role": …, …}        → 201 + one-time enrollment_code
DELETE /api/v1/orgs/<org>/devices/<id>/                                 → 204 (revoke)
POST /api/v1/devices/enroll/                      {"enrollment_code"}   → 200 + device_token (raw, once)
POST /api/v1/devices/unlock/                      {"pin"}               → 200 + session_token + role
POST /api/v1/checkins/                            {"entry_token", …}    → 200 (Authorization: Bearer <session>)
POST /api/v1/walkins/displays/next/                                     → 200 walk-in QR token + claim URL
POST /api/v1/e/<org>/<event>/claim/<token>/                             → 200 + info_form_url (anonymous)
POST /api/v1/e/<org>/<event>/info/<token>/        {form fields}         → 200 (anonymous, first-write-wins)

# Plan E
GET  /api/v1/orgs/<org>/events/<event>/guests/sync/?since=<iso>         → 200 + ETag (scanner session, 304 on match)
POST /api/v1/scanner/escalations/  {token, reason, original_payload…}   → 201 + escalation_id (scanner session)
# Plan E side effect on existing POST /api/v1/checkins/ — emits an additional
# checkin.conflict audit row when the duplicate's existing gate/scanner differs
# from the incoming attempt's. Plan F's help-desk inbox filters on it.

# Plan F
GET  /api/v1/orgs/<org>/events/<event>/helpdesk/tickets/?status=…       → 200 + ETag (org member, 304 on match)
POST /api/v1/orgs/<org>/events/<event>/helpdesk/tickets/<id>/claim/     → 200 + helpdesk.ticket_claimed audit
POST /api/v1/orgs/<org>/events/<event>/helpdesk/tickets/<id>/release/   → 200 + helpdesk.ticket_released audit
POST /api/v1/orgs/<org>/events/<event>/helpdesk/tickets/<id>/resolve/   → 200 + helpdesk.ticket_resolved audit
     body: {action: approve_checkin|resolved_with_note|void|escalated_to_manual_review, notes}
POST /api/v1/orgs/<org>/events/<event>/helpdesk/manual-review/<guest_id>/resolve/  → 200
     body: {action: approve_checkin|void, notes}  + helpdesk.manual_review_resolved audit
GET  /api/v1/orgs/<org>/events/<event>/audit/?action_prefix=…           → 200 + ETag (org member, 304 on match)
GET  /api/v1/orgs/<org>/events/<event>/stats/                           → 200 + ETag (org member, 7-field counts JSON)
GET  /api/v1/orgs/<org>/events/<event>/guests/?entry_status=…           → 200 (Task 11 added the filter)
POST /api/v1/scanner/escalations/   (extended)                          → 201 + writes BOTH audit row + HelpDeskTicketState in one txn
# Plan F also enforces append-only on audit_auditevent at the DB layer:
# direct UPDATE/DELETE attempts (even via flyctl ssh + psql) raise IntegrityError.
```

Backend test suite: **219 tests passing** (`uv run pytest`) — Plan E ended at 172; Plan F added 47 across helpdesk + audit + events stats + scanner + guests transitions + devices throttle, plus 2 regression tests from F+4. `uv run mypy apps/` clean across 119 source files. Frontend Vitest: **29 cases / 5 files** — Plan F added 10 across `__tests__/components/scanner/ios-install-banner.test.tsx` (3) + `mutation-queue.test.ts` reaper/dedupe/retry (~7 new cases).

---

## What's NOT done (parking lot heading into Plan G)

Sorted by impact for the door-day pilot.

**Plan G core (per brief §12 W12):**

1. **Telegram bot integration** — alternative check-in surface via Telegram bot for events where a dedicated scanner device is overkill (small events, hybrid online/offline guest lists).
2. **CSV guest import** — bulk-import guests from a CSV file. The legacy MVP supports this; SaaS rebuild needs parity for organizers migrating from Sheets.

**Plan F verification-driven follow-ups (small):**

3. **Frontend library duplication — pick one of `swr` or `@tanstack/react-query`.** Plan F Task 8 added `swr` for the helpdesk page; Tasks 9 + 10 used codebase-native TanStack Query. Both ship and work, but the dual-library state is debt. Pick one in a Plan G hygiene wave.
4. **Audit viewer `details_json` rendering.** The richest signal (escalation payload, ticket resolution notes) lives in `details_json` but is dropped from the audit table layout. An expandable row or modal-on-click pattern would unlock it. Operator-facing UX gap, not a blocker.
5. **Pre-commit hook gap — add `prettier --check` locally.** `.pre-commit-config.yaml` runs ESLint but not prettier; CI runs prettier and we hit this once during Plan F (commit `ed50eb1` was the prettier-format fix). One-line config addition.
6. **Verification checklist hygiene patches** (small refinements for the next walk-through):
   - **Cookie name** says `access`, actual is `eventgate_access` (settings `JWT_ACCESS_COOKIE`). Section 1 curl recipes need updating.
   - JWT access-token 15-min TTL trips long curl sessions. Document "refresh dashboard tab + re-paste cookie when curls return `Invalid token`."
   - DevTools IndexedDB panel doesn't auto-refresh after writes. Section 6's retry-failed test should mention the manual refresh icon OR the JS-console fallback query.
7. **Real cross-device `checkin.conflict` E2E re-verification.** Plan E verified this at `825b7e6`; Plan F's inbox depends on the signal but didn't re-confirm against current backend. Pre-pilot QA.
8. **Real walk-in display + claim + info form re-verification.** Same pre-pilot pattern as #7.

**Pre-pilot QA (Plan H or earlier):**

9. **`NEXT_PUBLIC_SENTRY_DSN` on Vercel** for production + preview. Browser Sentry SDK is wired (`sentry.client.config.ts` + `lib/scanner/sentry.ts`) but inert until set. DSN value is in Fly secrets (same DSN as backend).
10. **Branded PWA icons.** `frontend/public/icons/icon-{192,512}.png` are placeholder solid-color PNGs from `scripts/generate-placeholder-icons.mjs` (committed in `adbb3bc`). Replace once brand is decided — same file paths, no manifest change needed.
11. **Khmer translation review.** `frontend/lib/i18n/messages/km.json` strings (Plan D scanner + walkin + Plan E error messages) are still machine-quality.
12. **Resend sender-domain verification.** Currently sandbox-restricted to vinei.dev@gmail.com.
13. **Tighten Fly `ALLOWED_HOSTS`** from `*` to a specific allowlist.
14. **iOS PWA: 7-day cache eviction.** iOS clears PWA storage after ~7 days idle. Document in operator runbook: re-pair if PWA hasn't been opened in a week.
15. **iOS install banner real-iPhone confirmation** (Plan F Section 6a deferred). Code + Vitest behavioral tests pass; real-device confirmation pending.
16. **`in_flight` reaper repro on staging** (Plan F Section 6b deferred). Unit-tested via Vitest; manual reproduction is fiddly.
17. **Real Android Chrome E2E test.** Plan E was verified on desktop Chrome + iOS Chrome PWA. Android Chrome (likely the most common door-day device) wasn't exercised end-to-end.

**Hygiene:**

18. **Brand name + repo/app rename.** Once chosen: rename `eventgate` repo + Fly app + Vercel project + Sentry project + Resend domain.
19. **Token rotation.** Fly API token + Vercel token were pasted in prior chat sessions. Rotate when convenient.
20. **Pagination on `/guests/sync/`.** Defer until any event approaches 50k guests; ≤10k events fit fine in one response today.
21. **Pre-existing concurrency test flake.** `tests/test_checkin_concurrency.py::test_only_one_concurrent_checkin_wins`. Shared-DB connection leakage between concurrent transactional tests. Reproduces on `HEAD~N`.

**Plan F parking-lot items cleared by Plan F + F+ follow-ups:** ✅ Help-desk inbox UI (Task 8). ✅ Audit viewer UI (Task 9). ✅ Dashboard polling counts (Task 10). ✅ Manual-review queue UI (Task 11, F+3 producer). ✅ DB append-only trigger on `audit_events` (Task 1). ✅ Dedupe scan mutations (Task 0e). ✅ Backend auto-deploy hook (Task 0b). ✅ iOS install banner (Task 0c). ✅ `in_flight` mutation reaper (Task 0d). ✅ Retry-failed affordance (Task 0f). ✅ Online cache update (Task 0g). ✅ Per-IP rate limit on enroll (Task 0h). ✅ Verification-checklist patches to Plan E doc (Task 0a). ✅ Migrations auto-run on Fly deploy (`d9bbabd` post-verification).

**Plan E parking-lot items cleared by Plan F:** ✅ All 9 "deferred to Plan F" items from `2026-05-21-plan-e-verification-findings.md` shipped via the Task 0 wave and Plan F headline tasks.

The full eight-plan roadmap from the brief: A done, B done, C done, D done, E done, **F done**. **G = Telegram + CSV import. H = pilot QA hardening.**

---

## Working agreements (preserved across sessions)

### Brief Appendix A — firm product decisions (do NOT change without explicit reason)

- Pre-registered and walk-in flows stay separate.
- Pre-registered QR encodes the **raw `entry_token` only** (no URL wrapping).
- Pre-registered check-in mutation requires an **authorized staff scanner session**.
- Walk-in QR is **unique per guest** and persisted before display.
- Walk-in guests enter first, then complete info inside the hall.
- `entry_status` and `info_status` are **separate fields** with explicit transitions.
- No automatic walk-in token expiry.
- Duplicate / invalid / unauthorized cases route to **help desk lane**.
- Public root URL is the registration flow only; staff routes are gated.

### Engineering method

- **Brainstorm → write a plan → execute task-by-task.** Plans live in `docs/plans/YYYY-MM-DD-plan-X-name.md`. Each task has `- [ ]` checkboxes with explicit step-by-step code, file paths, commands, and expected output.
- **TDD discipline for backend.** Red → green → commit. Every task has a `tests/test_*.py` file. No skipping the red step.
- **Hybrid execution for plans of 20+ tasks.** Backend (TDD-shaped) tasks dispatched via Agent tool (`general-purpose`, sonnet) with full task text inline (never make the subagent read the plan). Frontend (component build) and deploy tasks done inline by the controller. After each backend task: spec-compliance review (sonnet) → code-quality review (sonnet) → fix → mark complete.
- **Frequent commits.** One commit per task, conventional-commit prefix (`feat(events): …`, `fix(cors): …`, `test(orgs): …`, `docs(plan-d): …`).
- **Pre-commit hooks** run ruff + ruff-format + ESLint + trailing-whitespace etc. If a hook modifies files, re-stage and re-commit; never use `--no-verify`.
- **Plans are living docs.** When execution surfaces a deviation, the implementer adjusts the code AND notes the deviation in the plan's completion log at the end. We don't silently diverge.
- **No new docs unless explicitly requested.** Stick to plans + commit messages.

### Local environment

- Python 3.12, `uv` (package manager, `.venv` per project), Django 5.
- Node 20 via nvm (`source ~/.nvm/nvm.sh && nvm use 20`).
- pnpm 9. Use `pnpm dlx vercel@latest` (no global install).
- Docker Desktop (Postgres + Redis via `docker-compose up -d` from the repo root).
- `flyctl auth login` to the `vinei.dev@gmail.com` Fly account.
- gh authenticated as both `vinei` and `vineidev` — the active one for git pushes is `vineidev` (SSH key `~/.ssh/github_vineidev`).
- Shell quirks: Bash cwd does NOT persist between commands. Use absolute paths or `&&`-chain.

### Files / boundaries that matter

- `backend/apps/common/` — cross-cutting only (`OrgScopedModel`, `IsOrgMember`/`HasOrgRole`, `tokens.py` for SHA-256 token helpers, `qr.py` for segno PNG). Do NOT bloat.
- `backend/apps/<feature>/` — each business domain (events, guests, accounts, orgs, notifications) is its own Django app, no cross-app circular imports.
- `backend/config/settings/{base,dev,prod,test}.py` — split settings. Tests use `config.settings.test` (locmem cache + email, eager Celery, in-DB Postgres).
- `frontend/app/(public)/` — anonymous pages (registration, walk-in claim).
- `frontend/app/(app)/` — authenticated dashboard.
- `frontend/app/scanner/` — Plan D + Plan E PWA scanner shell (`enroll`, `unlock`, `scan`, `walkin`, `escalations`). Workbox SW source at `frontend/sw-src/sw.ts`, build script at `frontend/scripts/build-sw.mjs`, IndexedDB layer at `frontend/lib/scanner/db.ts`. `frontend/sentry.client.config.ts` is scanner-only (dynamic-imported via `lib/scanner/sentry.ts`).
- `frontend/middleware.ts` — auth-redirect middleware. Matcher excludes `/api/*` (Vercel rewrites that to Django).
- `frontend/next.config.ts` — `trailingSlash: true`, `/api/*` rewrite to Fly, next-intl plugin wrapper.

---

## How to resume

1. `cd /Users/vinei/Projects/eventgate && git status && git log --oneline | head -30` — orient on the active checkout.
2. If continuing Plan N before merge, use
   `.claude/worktrees/plan-n-google-form-bridge` on
   `topic/plan-n-google-form-bridge`; top commit should be
   `1b8fc2a docs(plan-n): sharpen bridge cutoff gate` or newer.
3. If on merged `main`, read `docs/handoff-2026-06-03-post-plan-l-state.md`,
   this handoff's Plan N overlay, and
   `docs/plans/2026-06-07-plan-n-verification-checklist.md`.
4. Backend local commands may need `/Users/vinei/.local/bin/uv` if `uv` is not
   on PATH. Do not run multiple pytest processes in parallel against the
   reusable Postgres test DB.
5. Frontend commands need Node 20:
   `source ~/.nvm/nvm.sh && nvm use 20`.
6. **Most likely next task:** merge/deploy Plan N, then run the actual Google
   Form bridge rehearsal and record the June 12 T-7 go/no-go decision. After
   that, proceed with pilot dry-run/hardening or the post-pilot roadmap.

When writing future plans, follow the writing-plans skill structure (each task
with file paths, bite-sized steps, full code, expected output, no placeholders).
Execute subagent-driven where tasks are independent; use relative paths in
worker prompts; controller does spec + quality review before continuing.

### Pilot context

The first SaaS pilot window is **2026-06-19 → 2026-07-17** with Click Cam.
Plan N gives the team an optional Google Form intake bridge, but native
Eventgate registration and CSV import remain the fallback paths if the bridge is
not green by the **2026-06-12** T-7 gate.

---

## One-line summary

Plans A-L and pilot-feedback fixes are merged per
`docs/handoff-2026-06-03-post-plan-l-state.md`. Plan N is implemented and
verified locally on `topic/plan-n-google-form-bridge`: it adds the optional
one-way Google Form / response Sheet bridge into Eventgate guests, with hashed
secret auth, idempotent webhook processing, audit rows, QR email behavior,
settings UI, Apps Script guide, and T-7 verification gate. Next: merge/deploy
Plan N and run the real bridge rehearsal before deciding whether it is enabled
for the Click Cam pilot.
